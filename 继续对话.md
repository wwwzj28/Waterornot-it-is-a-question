### 🔹 建模项目总结

 A 题：透明塑料饮料瓶内残留液体识别。当前方案是 **两阶段模型**：

1. **YOLO11**：瓶体自动定位；
2. **ResNet50**：液体含量分类（empty / low / medium / high）。

项目目录规范如下：

```text
bottle_project/
│
├── data/
│   ├── raw_images/                 # 原始图片，支持 .jpg/.jpeg/.png/.bmp
│   ├── annotations_json/           # LabelMe JSON 标注文件
│   │
│   ├── yolo_detect/                # YOLO 检测数据集
│   │   ├── images/
│   │   │   ├── train/
│   │   │   ├── val/
│   │   │   └── test/
│   │   └── labels/
│   │       ├── train/
│   │       ├── val/
│   │       └── test/
│   │
│   └── resnet_cls/                 # ResNet50 分类数据集
│       ├── train/
│       │   ├── empty/
│       │   ├── low/
│       │   ├── medium/
│       │   └── high/
│       ├── val/
│       │   ├── empty/
│       │   ├── low/
│       │   ├── medium/
│       │   └── high/
│       └── test/
│           ├── empty/
│           ├── low/
│           ├── medium/
│           └── high/
│
├── configs/
│   ├── bottle_detect.yaml          # YOLO11 数据配置文件
│   └── train_config.yaml           # 通用训练配置文件
│
├── scripts/
│   ├── _paths.py                   # 项目路径统一管理
│   ├── 00_smoke_test.py            # 环境和目录快速测试
│   ├── 01_check_json.py            # 检查 LabelMe JSON 标注格式
│   ├── 02_split_dataset.py         # 划分 train/val/test 数据集
│   ├── 03_json_to_yolo.py          # JSON 标注转 YOLO txt 标签
│   ├── 04_train_yolo11.py          # 训练 YOLO11 检测模型
│   ├── 05_crop_bottle_by_label.py  # 使用人工标注框裁剪瓶体
│   ├── 06_crop_bottle_by_yolo.py   # 使用 YOLO 检测框裁剪瓶体
│   ├── 07_train_resnet50.py        # 训练 ResNet50 分类模型
│   ├── 08_eval_detection.py        # 评估 YOLO 检测效果
│   ├── 09_eval_classification.py   # 评估 ResNet50 分类效果
│   ├── 10_predict_pipeline.py      # 端到端预测流程
│   ├── 11_visualize_results.py     # 生成论文图表和可视化结果
│   └── 12_run_all.py               # 一键执行总控脚本
│
├── models/
│   ├── yolo/
│   │   └── yolo11_best.pt          # YOLO11 最佳权重
│   └── resnet/
│       └── resnet50_best.pth       # ResNet50 最佳权重
│
├── outputs/
│   ├── crops/                      # 裁剪后的瓶体图片
│   ├── figures/                    # 可视化图表
│   ├── metrics/                    # 评估指标和 CSV 结果
│   └── predictions/                # 端到端预测结果
│
├── runs/                           # 训练日志和中间输出
├── requirements.txt                # Python 依赖
├── README.md                       # 项目说明文档
├── AI_usage_statement.md           # AI 使用说明
├── run_all.bat                     # Windows 一键运行入口
└── run_all.sh                      # macOS/Linux 一键运行入口
```

---

### 🔹 JSON 格式规范（LabelMe）

* `shapes`：标注列表，每个 shape 是一个矩形框
* `label`：瓶子类别，正式标签：

  ```text
  bottle_empty, bottle_low, bottle_medium, bottle_high
  ```
* `points`：二维数组，左上角和右下角：

  ```python
  [[xmin, ymin], [xmax, ymax]]
  ```
* `imagePath`：图片文件名
* `imageWidth` / `imageHeight`：图片原始宽高

⚠ 分类阶段用 `CLASS_MAP` 映射：

```python
CLASS_MAP = {
    "bottle_empty": "empty",
    "bottle_low": "low",
    "bottle_medium": "medium",
    "bottle_high": "high"
}
```

---

### 🔹 脚本功能总结

| 脚本                         | 功能                                                                         |
| -------------------------- | -------------------------------------------------------------------------- |
| 01_check_json.py           | 检查 LabelMe JSON 是否规范（shapes / label / points / bbox范围 / imageWidth/Height） |
| 02_split_dataset.py        | 分层划分 train/val/test，生成 `split_list.csv`                                    |
| 03_json_to_yolo.py         | LabelMe JSON → YOLO txt 标签，复制图片到 `data/yolo_detect/images/`                |
| 04_train_yolo11.py         | 训练 YOLO11，训练后自动复制 `best.pt` 到 `models/yolo/yolo11_best.pt`                 |
| 05_crop_bottle_by_label.py | 使用人工框裁剪瓶体，生成 ResNet50 分类训练数据                                               |
| 06_crop_bottle_by_yolo.py  | 使用 YOLO11 框裁剪瓶体，用于端到端测试                                                    |
| 07_train_resnet50.py       | 训练 ResNet50 四分类模型，保存最佳权重 `models/resnet/resnet50_best.pth`                 |
| 08_eval_detection.py       | 评价 YOLO11 检测效果（IoU / Precision / Recall / CSV 输出）                          |
| 09_eval_classification.py  | 评价 ResNet50 分类效果（Accuracy / F1 / Confusion Matrix / CSV 输出）                |
| 10_predict_pipeline.py     | 完整端到端预测：YOLO → 裁剪 → ResNet50 → 输出预测 + 可视化                                  |
| 11_visualize_results.py    | 生成论文图表：数据集样例、YOLO检测、裁剪图、ResNet50训练曲线、混淆矩阵                                  |

---

### 🔹 图片处理说明

* `data/raw_images/`：放所有原始图片
* YOLO检测默认选择置信度最高框
* 裁剪图保存到 `outputs/crops/`
* 可视化图保存到 `outputs/figures/`
* CSV 保存预测结果和训练指标到 `outputs/metrics/`

---

### 🔹 开发顺序建议

```text
01_check_json.py → 02_split_dataset.py → 03_json_to_yolo.py → 05_crop_bottle_by_label.py → 
04_train_yolo11.py → 07_train_resnet50.py → 08_eval_detection.py → 09_eval_classification.py → 
06_crop_bottle_by_yolo.py → 10_predict_pipeline.py → 11_visualize_results.py
```

---

### 🔹 注意事项

1. 所有脚本读取路径都通过 `_paths.py`，保证统一
2. LabelMe JSON 必须与原图同名
3. YOLO阶段只识别类别 `bottle`，分类阶段再区分 empty/low/medium/high
4. 图片可能是 `.jpg` 或 `.jpeg`，脚本中已兼容
5. 批量预测默认遍历 `data/raw_images/`，单张预测可通过命令行参数指定

---